server {
    listen       443 ssl;
    server_name  %DOMAIN%;
    ssl_certificate     /nginx/ssl/%DOMAIN%-fullchain.cer;
    ssl_certificate_key  /nginx/ssl/%DOMAIN%.key;

    location / {
        root   /nginx/html;
        index  index.html index.htm;
    }
}